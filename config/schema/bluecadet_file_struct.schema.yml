bluecadet_file_struct.settings:
  type: config_object
  label: 'File struct Config settings'
  mapping:
    media_field:
      type: string
      label: 'Media Field to act on'
    public_vocab:
      type: string
      label: 'Vocabulary which represents the directory structure for public files.'
    private_vocab:
      type: string
      label: 'Vocabulary which represents the directory structure for private files.'
